<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo_10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RandomIncident"></activity>
        <activity android:name=".AddCar" />
        <activity android:name=".SignUp" />
        <activity android:name=".LogIn" />
        <activity android:name=".MyInsuranceInfo" />
        <activity
            android:name=".MyInformation"
            android:label="@string/title_activity_my_information"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewPastIncidents"
            android:label="@string/title_activity_view_pat_incidents"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LogNewIncident" />
        <activity android:name=".Menu" />
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>